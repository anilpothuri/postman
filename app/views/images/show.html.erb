<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @image.name %>
</p>

<p>
  <strong>Picture:</strong>
  <%=image_tag(@image.picture.url,size:"400x350") if @image.picture? %>
</p>

<p>
  <strong>Article:</strong>
  <%= @image.article_id %>
</p>

<%= link_to 'Edit', edit_image_path(@image) %> |
<%= link_to 'Back', images_path %>
